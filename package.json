{
  "name": "clauclau",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://nicolaslauzon.github.io",
  "scripts": {
    "deploy": "rm -rf .deploy && mkdir .deploy && rsync -a --exclude '.deploy' --exclude '.git' --exclude 'mtb' --exclude 'node_modules' --exclude '.next' --exclude 'dist' --exclude 'npm-debug.log*' ./ .deploy/ && gh-pages -d .deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
