export default function PortfolioSection() {
  return (
    <section id="portfolio" className="relative h-screen flex items-center justify-center snap-start snap-always bg-neutral-950">
      <div className="relative z-10 flex flex-col items-center justify-center w-full h-full">
        <h1 className="text-6xl md:text-8xl lg:text-9xl font-bold text-white text-center drop-shadow-2xl mb-8">Portfolio</h1>
        {/* Add your portfolio content here */}
      </div>
    </section>
  );
}
